# Contributron Web Configuration

# Web server port (default: 3000)
PORT=3000

# Maximum file upload size in MB (default: 10MB)
MAX_UPLOAD_SIZE=10

# Temporary directories (these are created automatically)
# UPLOADS_DIR=uploads
# GENERATED_DIR=generated